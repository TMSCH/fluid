# Your OpenAI API key. The app will skip the setup screen when this is set.
# On mobile (iOS/Android), API calls go directly to OpenAI.
# On web, you need the CORS proxy: node scripts/api-proxy.js
EXPO_PUBLIC_OPENAI_API_KEY=sk-your-key-here
